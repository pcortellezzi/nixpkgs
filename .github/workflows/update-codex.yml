name: Update codex package

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * *'

jobs:
  update-codex:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}

      - name: Install Nix
        uses: cachix/install-nix-action@v27
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Run update script
        run: bash pkgs/codex/update.sh

      - name: Commit changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          if ! git diff --quiet pkgs/codex/default.nix; then
            NEW_VERSION=$(grep 'version =' pkgs/codex/default.nix | head -1 | cut -d'"' -f2)
            git add pkgs/codex/default.nix
            git commit -m "codex: auto-update to $NEW_VERSION"
            git push
          else
            echo "codex is already up to date."
          fi
